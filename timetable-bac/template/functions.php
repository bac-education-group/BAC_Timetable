<?php
if ( ! defined( 'ABSPATH' ) ) exit; // Exit if accessed directly
if ( ! defined( 'WPINC' ) ) { die; }

add_action( 'course_lists', 'course_lists' );
function course_lists($content) {
	$content = 'figjfghkj';

	return $content;
}
